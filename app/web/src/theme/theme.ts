import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#53E79C'
    },
    secondary: {
      main: '#43BAFF'
    },
    background: {
      default: '#1A1B1C'
    }
  }
})

export default theme
